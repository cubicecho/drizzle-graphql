node_modules
dist

# The build writes into dist/ and nowhere else, so root-level bundle output is always
# stale. A tracked copy lived here until it was removed; these are anchored to the repo
# root so a stray build cannot quietly re-add it, and so they never shadow src/ or dist/.
/index.js
/index.cjs
/index.d.ts
/index.d.cts
/index.js.map
/index.cjs.map

*.sqlite
drizzle.config.ts
*.tgz
temp.ts
dist.new
server
runMysql.ts
runMysql-old.ts
runPg.ts
tests/.temp
tests/Migrations
.DS_Store
drizzle.test-heavy.ts
changes.md
tests/isolated.test.ts
coverage
